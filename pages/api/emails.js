const {
  buildConfirmationBody,
  buildNotificationBody,
  buildConfirmationSubject,
} = require("./_lib/confirmation-email");

module.exports = {
  buildConfirmationBody,
  buildNotificationBody,
  buildConfirmationSubject,
};